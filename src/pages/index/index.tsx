import { View, CustomWrapper } from "@tarojs/components";

export default function Index() {
  return (
    <View className="page">
      <CustomWrapper>
        <n-view />
      </CustomWrapper>
    </View>
  );
}
